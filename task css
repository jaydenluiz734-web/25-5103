/* Reset & base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
    color: #333;
    line-height: 1.6;
    min-height: 100vh;
}

/* ── Heading styles using classes & IDs ── */
#main-title {
    font-size: 3em;
    margin-bottom: 10px;
    background: linear-gradient(to right, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: float 3s ease-in-out infinite;
}

.hero-subtitle {
    font-size: 1.8em;
    margin-bottom: 10px;
    font-weight: 600;
    color: #2d3436;
}

.highlight-statement {
    font-size: 1.3em;
    margin-bottom: 10px;
    font-weight: 500;
    color: #d63031;
}

.local-focus {
    font-size: 1.1em;
    margin-bottom: 20px;
    font-weight: 500;
    color: #0984e3;
}

.quote-title {
    font-size: 2.1em;
    color: #2d3436;
    margin: 20px 0;
    font-weight: bold;
}

/* Section headings (most h2s in cards) */
.section-title {
    color: #2d3436;
    margin-bottom: 25px;
    font-size: 1.6em;
    position: relative;
    display: inline-block;
}

.section-title::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 50px;
    height: 3px;
    background: linear-gradient(90deg, #ff6b6b, #feca57);
    border-radius: 2px;
}

/* Hero specific overrides */
.hero h1, .hero h2, .hero h3, .hero h4, .hero .tagline {
    position: relative;
    z-index: 1;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
}

/* Keep all your other styles (hero, nav, card, table, gallery, etc.) */
/* Just make sure to remove or adjust any remaining direct h1, h2, h3, h4 selectors */

.hero {
    background: linear-gradient(135deg, #ff6b6b 0%, #feca57 50%, #48dbfb 100%);
    color: white;
    text-align: center;
    padding: 80px 20px;
    max-width: 1000px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
}

/* ... rest of your beautiful styles ... */

/* Footer styles moved from inline to here */
footer {
    margin-top: 60px;
    padding: 40px 20px;
    background: linear-gradient(135deg, #1a1a2e 0%, #0f3460 100%);
    border-top: 3px solid #ff6b6b;
    text-align: center;
    border-radius: 15px 15px 0 0;
    position: relative;
    overflow: hidden;
}

/* ... continue moving other inline styles to classes ... */
